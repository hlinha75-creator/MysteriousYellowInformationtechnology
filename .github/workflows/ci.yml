name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: Testes e qualidade (Node 22)
    runs-on: ubuntu-latest

    steps:
      - name: Baixar codigo
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Instalar dependencias
        run: npm ci

      - name: Verificar sintaxe
        run: npm run test:syntax

      - name: Executar testes com cobertura minima
        run: npm run test:coverage

      - name: Auditar vulnerabilidades de producao
        run: npm audit --omit=dev --audit-level=high
